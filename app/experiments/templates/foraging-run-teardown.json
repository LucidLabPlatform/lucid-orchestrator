{
  "id": "foraging-run-teardown",
  "name": "Foraging Run Teardown",
  "version": "1.0.0",
  "description": "Disables foraging-phase telemetry. Topic links are auto-deleted.",
  "tags": ["foraging", "teardown"],
  "parameters": {
    "robot_id":       { "type": "string",  "default": "rosbot", "required": true,  "description": "ROSbot agent ID" },
    "hydra_agent_id": { "type": "string",  "default": "hydra",  "required": true,  "description": "Hydra agent" },
    "launch_rosbot":  { "type": "boolean", "default": true,     "required": false, "description": "Disable rosbot foraging telemetry" },
    "launch_hydra":   { "type": "boolean", "default": true,     "required": false, "description": "Disable hydra OptiTrack telemetry" },
    "led_agent_id":   { "type": "string",  "default": "led_truss", "required": false, "description": "LED strip agent" },
    "launch_led":     { "type": "boolean", "default": true,     "required": false, "description": "Remove LED topic link" }
  },
  "steps": [
    {
      "name": "remove-foraging-mode-led-link",
      "type": "topic_link",
      "when": "${launch_led}",
      "on_failure": "continue",
      "operation": "delete",
      "source_topic": "lucid/agents/${robot_id}/components/ros_bridge/telemetry/foraging_status",
      "target_topic": "lucid/agents/${led_agent_id}/components/led_strip/cmd/set-color",
      "select_clause": "*",
      "qos": 0
    },

    {
      "name": "disable-foraging-telemetry",
      "type": "parallel",
      "on_failure": "continue",
      "steps": [
        { "name": "disable-optitrack-telemetry",         "type": "command", "when": "${launch_hydra}",  "on_failure": "continue", "agent_id": "${hydra_agent_id}", "component_id": "ros_bridge", "action": "cfg/telemetry/set", "timeout_s": 10, "params": { "set": { "optitrack_umh_4": { "enabled": false } } } },
        { "name": "disable-rosbot-foraging-telemetry",   "type": "command", "when": "${launch_rosbot}", "on_failure": "continue", "agent_id": "${robot_id}",       "component_id": "ros_bridge", "action": "cfg/telemetry/set", "timeout_s": 10, "params": { "set": { "puck_registry": { "enabled": false }, "aruco_registry": { "enabled": false }, "odom": { "enabled": false }, "foraging_status": { "enabled": false } } } }
      ]
    }
  ]
}

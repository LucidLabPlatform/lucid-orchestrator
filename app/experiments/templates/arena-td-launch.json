{
  "id": "arena-td-launch",
  "name": "Arena TD Launch",
  "version": "1.0.0",
  "description": "Launches TouchDesigner and the Arena overlay on socrates, then dims room lights.",
  "tags": ["setup", "arena", "touchdesigner"],
  "parameters": {
    "socrates_agent_id":   { "type": "string", "default": "socrates", "required": true,  "description": "Agent ID running the viz component (TouchDesigner + Arena overlay)" },
    "hydra_agent_id":      { "type": "string", "default": "hydra",    "required": true,  "description": "Agent ID for the Hydra node (hosts KNX lighting component)" },
    "room_brightness":     { "type": "integer","default": 1,          "required": false, "description": "Room light brightness 0-100 (1 = dim for experiments)" },
    "arena_anchor_corner": { "type": "string", "default": "BL",                          "required": false, "description": "Which arena corner is anchored in OptiTrack (TL/TR/BR/BL)" },
    "arena_anchor_x":      { "type": "float",  "default": 3.292316246032715,             "required": false, "description": "Anchor corner X in OptiTrack frame" },
    "arena_anchor_y":      { "type": "float",  "default": 2.127694296836853,             "required": false, "description": "Anchor corner Y in OptiTrack frame" },
    "goal_x":              { "type": "float",  "default": 2.992316246032715,             "required": false, "description": "Robot start X (OptiTrack frame)" },
    "goal_y":              { "type": "float",  "default": 1.827694296836853,             "required": false, "description": "Robot start Y (OptiTrack frame)" },
    "goal_qx":             { "type": "float",  "default": -0.00494308490306139,          "required": false, "description": "Robot start orientation qx" },
    "goal_qy":             { "type": "float",  "default": -0.011712746694684029,         "required": false, "description": "Robot start orientation qy" },
    "goal_qz":             { "type": "float",  "default": -0.9295409917831421,           "required": false, "description": "Robot start orientation qz" },
    "goal_qw":             { "type": "float",  "default": 0.3684997856616974,            "required": false, "description": "Robot start orientation qw" }
  },
  "steps": [
    {
      "name": "start_touchdesigner",
      "type": "command",
      "on_failure": "abort",
      "retries": 1,
      "agent_id": "${socrates_agent_id}",
      "component_id": "viz",
      "action": "start_touchdesigner",
      "timeout_s": 30,
      "params": {}
    },
    {
      "name": "start_arena",
      "type": "command",
      "on_failure": "abort",
      "retries": 1,
      "agent_id": "${socrates_agent_id}",
      "component_id": "viz",
      "action": "start_arena",
      "timeout_s": 20,
      "params": {
        "arena_anchor_corner": "${arena_anchor_corner}",
        "arena_anchor_x":      "${arena_anchor_x}",
        "arena_anchor_y":      "${arena_anchor_y}",
        "goal_x":  "${goal_x}",
        "goal_y":  "${goal_y}",
        "goal_qx": "${goal_qx}",
        "goal_qy": "${goal_qy}",
        "goal_qz": "${goal_qz}",
        "goal_qw": "${goal_qw}"
      }
    },
    {
      "name": "set_room_lights",
      "type": "command",
      "on_failure": "continue",
      "retries": 1,
      "agent_id": "${hydra_agent_id}",
      "component_id": "knx",
      "action": "light/brightness/set",
      "timeout_s": 10,
      "params": { "light": "Experiments light", "brightness": "${room_brightness}" }
    }
  ]
}
